/**
 * The hero deliberately leaves the reading field clear. Space depth comes
 * from the real Earth, asteroid GLB, and sparse 3D stars in the canvas; this
 * remains an integration point for both hero paths without adding a DOM fog
 * layer behind the copy.
 */
export default function CosmicScene(): null {
  return null;
}
